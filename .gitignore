# Python
__pycache__/
*.pyc
*.egg-info/

# Build outputs (anchored to the repo root so they cannot swallow
# paths like config/includes.chroot/usr/local/bin)
/build/
/dist/
/.build/
/cache/
/chroot/
/binary/
/local/
/build.log
*.iso
*.iso.download

# stale local wheel build tree
installer/build/
