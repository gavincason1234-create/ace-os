# ACE OS tuning for a 4 GB RAM machine with zram swap.
# High swappiness is correct with zram: swapping to compressed RAM is cheap,
# and it keeps the file cache warm on the slow eMMC.
vm.swappiness=100
